import type { RunTimeLayoutConfig } from '@umijs/max';

export const layout: RunTimeLayoutConfig = () => {
  return {
    title: 'WFP销售人员诊断中心',
    logo: '/logo.svg',
    menu: {
      locale: false,
    },
  };
};
